// Centralize module exports if needed
import dotenv from "dotenv";

// Load environment variables from .env file
dotenv.config();

// Optionally, export configurations or commonly used utilities

export default {}; // Export a default empty object if there are no other exports
