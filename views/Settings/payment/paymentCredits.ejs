<div class="w-full px-3 bg-white">
	<div class="flex flex-col w-full items-center">
		<div class="w-full h-full flex justify-center box-content rounded-xl" id="right-side">
			<div class="w-9/12">
				
				<div class="flex items-center justify-between my-8">
					<button type="button" class="<%= paymentTableData?.length > 0 ? '' : 'hidden' %>flex items-center  text-gray-500" onclick="location.reload()"><span class="mb-1 mr-1">&LeftArrow;</span> Go Back</button>
					<h1 class="text-2xl text-center font-semibold">Payment Credits</h1>
					<div class="invisible">payment</div>
				</div>
				
				<!-- Plan Selection -->
				<div class="grid grid-cols-2 gap-10">
					
					<label class="radio-option block cursor-pointer grow">
						<input type="radio" name="credits" value="weekday" class="sr-only">
						<div class="option-content border-2 border-gray-200 rounded-xl p-6 hover:border-gray-300">
							<div class="flex items-center space-x-4 w-full">
								<div class="w-full space-y-8">
									<input type="number" hidden value="1000" />
									<div class="flex justify-between items-center">
										<h3 class="text-2xl font-semibold text-gray-900">Starter</h3>
										<h3 class="text-2xl font-semibold text-gray-900">1000 <span class="">messages</span></h3>
									</div>
									
									<div class="flex items-center justify-between my-3">
										<div>
											<div class="text-lg text-red-500 line-through"><span class=" mr-1 text-gray-500"><%= credits[1000].currency %></span><%= credits[1000].amount * 3 %></div>
											<div class="text-2xl font-bold text-gray-900"><span class=" mr-1 text-gray-500"><%= credits[1000].currency %></span><%= credits[1000].amount %></div>
										</div>
										<div class="text-right">
											<div class="text-lg text-red-500 line-through"><span class=" mr-1 text-gray-500"><%= credits[1000].currency %></span><%= (credits[1000].amount * 3 / 1000).toFixed(2) %> <span>/message</span></div>
											<div class="text-2xl font-bold text-gray-900"><span class=" mr-1 text-gray-500"><%= credits[1000].currency %></span><%= (credits[1000].amount / 1000).toFixed(2) %> <span>/message</span></div>
										</div>
									</div>
									
								</div>
								
							</div>
							
						</div>
						
					</label>
					
					<label class="radio-option block cursor-pointer grow">
						<input type="radio" name="credits" value="weekday" class="sr-only">
						<div class="option-content border-2 border-gray-200 rounded-xl p-6 hover:border-gray-300">
							<div class="flex items-center space-x-4 w-full">
								<div class="w-full space-y-8">
									<input type="number" hidden value="3000" />
									<div class="flex justify-between items-center">
										<h3 class="text-2xl font-semibold text-gray-900">Catch</h3>
										<h3 class="text-2xl font-semibold text-gray-900">3000 <span class="">messages</span></h3>
									</div>
									<div class="flex items-center justify-between my-3">
										<div>
											<div class="text-lg text-red-500 line-through"><span class=" mr-1 text-gray-500"><%= credits[3000].currency %></span><%= credits[3000].amount * 3 %></div>
											<div class="text-2xl font-bold text-gray-900"><span class=" mr-1 text-gray-500"><%= credits[3000].currency %></span><%= credits[3000].amount %></div>
										</div>
										<div class="text-right">
											<div class="text-lg text-red-500 line-through"><span class=" mr-1 text-gray-500"><%= credits[3000].currency %></span><%= (credits[3000].amount * 3 / 3000).toFixed(2) %> <span>/message</span></div>
											<div class="text-2xl font-bold text-gray-900"><span class=" mr-1 text-gray-500"><%= credits[3000].currency %></span><%= (credits[3000].amount / 3000).toFixed(2) %> <span>/message</span></div>
										</div>
									</div>
									
								</div>
								
							</div>
							
						</div>
						
					</label>
					
					<label class="radio-option block cursor-pointer grow">
						<input type="radio" name="credits" value="unlimited" class="sr-only" checked>
						<div class="option-content border-2 border-gray-200 rounded-xl p-6 hover:border-gray-300">
							<div class="flex items-center justify-between">
								<div class="flex items-center space-x-4 w-full">
									<div class="w-full space-y-8">
										<input type="number" hidden value="5000" />
										<div class="flex justify-between items-center">
											<h3 class="text-2xl font-semibold text-gray-900">Growth</h3>
											<h3 class="text-2xl font-semibold text-gray-900">5000 <span class="">messages</span></h3>
										</div>
										<div class="flex items-center justify-between my-3">
											<div>
												<div class="text-lg text-red-500 line-through"><span class=" mr-1 text-gray-500"><%= credits[5000].currency %></span><%= credits[5000].amount * 3 %></div>
												<div class="text-2xl font-bold text-gray-900"><span class=" mr-1 text-gray-500"><%= credits[5000].currency %></span><%= credits[5000].amount %></div>
											</div>
											<div class="text-right">
												<div class="text-lg text-red-500 line-through"><span class=" mr-1 text-gray-500"><%= credits[5000].currency %></span><%= (credits[5000].amount * 3 / 5000).toFixed(2) %> <span>/message</span></div>
												<div class="text-2xl font-bold text-gray-900"><span class=" mr-1 text-gray-500"><%= credits[5000].currency %></span><%= (credits[5000].amount / 5000).toFixed(2) %> <span>/message</span></div>
											</div>
										</div>
										
									</div>
									
								</div>
								
							</div>
						</div>
					</label>
	
					<label class="radio-option block cursor-pointer grow">
						<input type="radio" name="credits" value="extended" class="sr-only">
						<div class="option-content border-2 border-gray-200 rounded-xl p-6 hover:border-gray-300">
							<div class="flex items-center justify-between">
								<div class="flex items-center space-x-4 w-full">
									<!-- <div class="check-indicator"></div> -->
									<div class="w-full space-y-8">
										<input type="number" hidden value="10000" />
										<div class="flex justify-between items-center">
											<h3 class="text-2xl font-semibold text-gray-900">Scale</h3>
											<h3 class="text-2xl font-semibold text-gray-900">10000 <span class="">messages</span></h3>
										</div>
										<div class="flex items-center justify-between my-3">
											<div>
												<div class="text-lg text-red-500 line-through"><span class=" mr-1 text-gray-500"><%= credits[10000].currency %></span><%= credits[10000].amount * 3 %></div>
												<div class="text-2xl font-bold text-gray-900"><span class=" mr-1 text-gray-500"><%= credits[10000].currency %></span><%= credits[10000].amount %></div>
											</div>
											<div class="text-right">
												<div class="text-lg text-red-500 line-through"><span class=" mr-1 text-gray-500"><%= credits[10000].currency %></span><%= (credits[10000].amount * 3 / 10000).toFixed(2) %> <span>/message</span></div>
												<div class="text-2xl font-bold text-gray-900"><span class=" mr-1 text-gray-500"><%= credits[10000].currency %></span><%= (credits[10000].amount / 10000).toFixed(2) %> <span>/message</span></div>
											</div>
										</div>
									</div>
								</div>
								
							</div>
							
						</div>
						
					</label>
				</div>
			</div>
		</div>
		
		<div class="w-1/2 flex justify-center">
			<div class="space-y-4 py-10" id="btn-wrap">
				
				<button type="button" class="bg-black rounded h-10 w-52 flex items-center justify-center" id="btn">
					<div class="text-white loader-text">Continue with Payment</div>
					<div class="loading-spinner hidden"></div>
				</button>
				
			</div>
			
		</div>
	</div>
</div>